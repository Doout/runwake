# Remote agents

Remote agents are not available in Runwake `0.1.x`.

The implementation remains under development and is excluded from the UI, API routes, binaries, container builds, and release archives. It is planned for a later release after its enrollment, lifecycle, upgrade, and recovery behavior is ready for support.
